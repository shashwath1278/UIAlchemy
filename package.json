{
  "name": "create-ui-app-cli",
  "version": "1.0.4",
  "description": "CLI tool to create React apps with popular UI libraries",
  "type": "commonjs",
  "bin": {
    "create-ui-app": "./dist/cli.js"
  },
  "dependencies": {
    "inquirer": "^8.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  },
  "scripts": {
    "clean": "if exist dist rd /s /q dist",
    "make-dist": "if not exist dist mkdir dist",
    "build": "npm run clean && npm run make-dist && esbuild src/index.js --bundle --platform=node --format=cjs --outfile=dist/cli.js --external:create-ui-app-cli/package.json && node scripts/fix-shebang.js && npm run copy-presets",
    "copy-presets": "node scripts/copy-presets.js",
    "test": "node test-runner.js",
    "setup": "npm cache clean --force && npm install && npm run build && npm uninstall -g create-ui-app-cli && npm link",
    "reset": "rd /s /q node_modules 2>nul & rd /s /q dist 2>nul & npm run setup"
  },
  "keywords": [
    "react",
    "ui",
    "cli",
    "chakra-ui",
    "material-ui",
    "ant-design",
    "bootstrap",
    "primereact",
    "tailwindcss"
  ],
  "author": "shashwath1278",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/shashwath1278/reactify-ui.git"
  },
  "bugs": {
    "url": "https://github.com/shashwath1278/reactify-ui/issues"
  },
  "homepage": "https://github.com/shashwath1278/reactify-ui#readme",
  "files": [
    "dist",
    "presets"
  ]
}
