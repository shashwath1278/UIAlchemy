{
  "name": "create-ui-app-cli",
  "version": "1.0.2",
  "type": "module",
  "bin": {
    "create-ui-app": "./dist/cli.cjs"
  },
  "dependencies": {
    "inquirer": "^9.3.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --format=cjs --outfile=dist/cli.js --banner:js=\"#!/usr/bin/env node\""
  }
}
